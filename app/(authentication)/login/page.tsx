import React from 'react';
import AuthForm from '@/features/auth/ui/auth-form';

const Auth = () => {
    return (
        <div>
            <AuthForm />
        </div>
    );
};

export default Auth;
